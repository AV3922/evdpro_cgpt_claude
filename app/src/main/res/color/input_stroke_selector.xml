<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Focused -->
    <item android:color="@color/brand_blue" android:state_focused="true"/>

    <!-- Activated -->
    <item android:color="@color/brand_blue" android:state_activated="true"/>

    <!-- Default -->
    <item android:color="@color/light_divider"/>

</selector>